{
  "name": "tauritavern-easy-access-logs",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check index.js && node --check log-utils.js && node --test"
  }
}
